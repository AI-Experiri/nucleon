//! The `nucleon` CLI — chapter 9. Grows real subcommands in the chapter-9
//! deep dive (`nucleon run --model <dir> --prompt "…"`).

fn main() {
    println!(
        "nucleon {} — scaffold; see docs/plan.md",
        env!("CARGO_PKG_VERSION")
    );
}
